__pycache__/
*.pyc
.venv/
venv/
.ipynb_checkpoints/
.DS_Store

# secrets de Streamlit (credenciales GEE) - nunca commitear, solo .streamlit/secrets.toml.example
.streamlit/secrets.toml

# snapshot de ranchos/clientes - contiene nombre, telefono y ubicacion de finca de cada cliente,
# se genera aparte (ver README) y nunca se commitea
data/*.gpkg
data/*.geojson

# estado en vivo del ranking de riesgo (para las flechas de subida/bajada) - se regenera solo
data/ranking_historial.json
